<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- Main Content Area -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Top App Bar with ☰ Hamburger Menu -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topToolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/slate_950"
            android:elevation="4dp"
            app:navigationIcon="@drawable/ic_menu_hamburger"
            app:navigationIconTint="@color/white"
            app:title="Smart Solar"
            app:titleTextColor="@color/white" />

        <!-- Screen Content Container (Home / Fragments) -->
        <FrameLayout
            android:id="@+id/fragmentContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/background_light">

            <!-- Clean Placeholder for Home Screen -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:orientation="vertical"
                android:padding="32dp">

                <FrameLayout
                    android:layout_width="92dp"
                    android:layout_height="92dp"
                    android:background="@drawable/bg_logo_glow"
                    android:elevation="6dp"
                    android:padding="12dp">

                    <ImageView
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:contentDescription="@string/brand_name"
                        android:scaleType="fitCenter"
                        android:src="@drawable/logo3" />
                </FrameLayout>

                <TextView
                    android:id="@+id/tvCurrentScreenTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="20dp"
                    android:fontFamily="sans-serif-black"
                    android:text="Welcome to SolarGrid"
                    android:textColor="@color/slate_900"
                    android:textSize="22sp" />

                <TextView
                    android:id="@+id/tvScreenDescription"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:fontFamily="sans-serif"
                    android:gravity="center"
                    android:lineSpacingMultiplier="1.2"
                    android:text="Tap the ☰ menu button in the top bar\nto open the Navigation Drawer"
                    android:textColor="@color/slate_500"
                    android:textSize="14sp" />
            </LinearLayout>

        </FrameLayout>

    </LinearLayout>

    <!-- Professional Left Sliding Navigation Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/surface_card"
        android:fitsSystemWindows="true"
        app:elevation="16dp"
        app:headerLayout="@layout/nav_header_main"
        app:itemBackground="@drawable/bg_nav_item_selector"
        app:itemIconPadding="18dp"
        app:itemIconSize="22dp"
        app:itemIconTint="@color/nav_item_tint_selector"
        app:itemShapeInsetEnd="12dp"
        app:itemShapeInsetStart="12dp"
        app:itemTextColor="@color/nav_item_text_selector"
        app:menu="@menu/drawer_menu"
        app:subheaderColor="@color/slate_500"
        app:subheaderTextAppearance="@style/Widget.SmartSolar.NavigationView.Subheader" />

</androidx.drawerlayout.widget.DrawerLayout>
