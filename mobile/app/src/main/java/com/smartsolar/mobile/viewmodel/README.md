# ViewModels (`viewmodel`)

This package contains Android Architecture Component ViewModels managing UI-related data lifecycle.

## Planned Files
- **`StationViewModel.kt`**: Manages state, LiveData / StateFlow for station lists, search results, and station details.
- **`SlotViewModel.kt`**: Manages slot fetching, date filtering, and booking capacity actions.
