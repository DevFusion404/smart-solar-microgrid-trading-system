# Adapters (`ui/adapter`)

This package contains RecyclerView Adapters and ViewHolders for list presentations.

## Planned Files
- **`StationAdapter.kt`**: Adapter for rendering solar station cards in RecyclerView (`item_station.xml`).
- **`SlotAdapter.kt`**: Adapter for rendering available energy booking slot items (`item_slot.xml`).
