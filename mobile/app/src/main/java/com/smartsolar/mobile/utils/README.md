# Utilities (`utils`)

This package contains helper functions, constants, and network utilities.

## Planned Files
- **`Constants.kt`**: Application-wide constants including API base URLs, shared preference keys, and intent extras.
- **`NetworkUtils.kt`**: Helper utilities for internet connectivity checks and network status listeners.
