# Repositories (`data/repository`)

This package implements the Repository pattern, serving as a single source of truth for application data.

## Planned Files
- **`StationRepository.kt`**: Coordinates data operations for solar stations (fetching list, station details, map pins, search).
- **`SlotRepository.kt`**: Coordinates slot fetching, date filtering, and booking capacity updates.
